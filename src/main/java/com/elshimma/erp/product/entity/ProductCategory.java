package com.elshimma.erp.product.entity;

/**
 * Product categories matching El Shimma's business lines.
 *
 * FABRIC  — raw fabrics (gabardine, etc.) sold by meter/kg
 * UNIFORM — finished uniforms (school, military, hotel, company)
 * BADGE   — woven/embroidered patches and badges
 * LABEL   — woven care/brand labels (tickets)
 */
public enum ProductCategory {
    FABRIC,
    UNIFORM,
    BADGE,
    LABEL
}
